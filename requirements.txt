python-telegram-bot>=22.0
# httpx comes with python-telegram-bot; listed because bot.py imports it directly (transcription API)
httpx
# [default] adds the YouTube JS-challenge solver, [deno] bundles the JavaScript runtime it needs
yt-dlp[default,deno]>=2026.1.1
faster-whisper>=1.1.0
# numpy and av come with faster-whisper; listed because bot.py imports them directly (screenshots)
numpy
av
anthropic>=1.0.0
pydantic>=2.0
python-dotenv>=1.0.0
